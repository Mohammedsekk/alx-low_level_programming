#include<stdio.h>
/**
  * main - prints its name, followed by a new line.
  * @argc : integer
  * @argv : string
  * Return: Always 0 (Success)
  */
int main(char argc, char **argv)
{
(void)argc;
printf("%s\n", argv[0]);
return (0);
}
